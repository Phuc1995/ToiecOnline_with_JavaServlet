-HQL:
	+ StringBuildere
-Hibernate
-Testng
-TomCat
-UrlPatterns
-SQL
-Jquery:
	+Jquery each
		1.$.each(array, function(index, value){
				cosole.log("Value: "+ value +"- position: "+ index);
			});
	+$('#sex').on('change'), function(){}
	+.attr('id') == "checkAll"
	+.prop('checked' == false);
	+.closest('table').find('type=checkbox').prop('checked', false);
	+load method()
-Apache Commons Upload File:
	+ enctype="multipart/form-data"
-HttpSession	
-Displaytag
-Log4j:  BasicConfigurator.configure();
-SiteMesh/Decorator:
	+ page cha(master layout)
	+ page con()
-JSTL(JSP Standard Tag Library): thu vien cua JSP, duoc build san, nhung thu vien tu viet ra. 
	+ <c:url>
	+ <c:choose>
		<c:when>
		<c:otherwise>
	  </c:choose>
-Taglib:
	+ fmt:setBundle
-MVC:
	+Model(command):trung gian giua controller va view, 
	+View
	+Controller
	+ model - view - controller
	  controller -->(model-command) view
	  view --> (model) controller
	+ DAO: chua cau truy van, ket noi databases. Co 1 function clean code-->service co nhieu function use from 1 function DAO
	+ SERVICE -> nhieu ham return ham tu tang DAO -> trong service chua logic code
-DTO: data tranfer object- Java bean(implement seriliable):
	+ Cotent model object
	+ private property
	+ Getter and setter
	+ extend
	+ implement
-Pojo: plain old java object	
	+ command: save all data from from views
	+ class java bean thuan, not implement and extend(implement seriliable)
	+ command -> pojo -> DTO
	+ UserDTO dto = new UserDTO();
	  dto.getName();
	  this.pojo = new UserDTO();
	  pojo.name;
-Function findByproperties(core-web -> util --> initSearchBean) need to 4 agument
	+ sortExpression - can ten cua column muon sort
	+ sortDirection - can tra ve gia tri 1 or 2
	+ firstItem - can tra ve gia tri
	+  String sortExpression = request.getParameter(new ParamEncoder(bean.getTableId()).encodeParameterName(TableTagParameters.PARAMETER_SORT));
	+ StringUtils.isNotBlank(pageStr): check different ""(emty);
-List hien tai -> List<ListenGuidelineEntity>
	+ list(DTO)
	+ trong Service: chuyen List<ListenGuidelineEntity> --> List<ListenGuideLineDTO>
-Collection:
	+ List <String> list = new ArrayList<>(): 
		1.Dong lap for: chay tu dau den cuoi stop when use break,
	+ HashSet Set<String> set = new HashSet<>(): chi chua phan tu duy nhat khong trung value, lay value cuoi cung, 
		1.Dong lap for(Book b:set): find theo key.
-Map: 
	+ HashMap<Integer, String> map = new HashMap<Integer, String>
		1.for(Map.Entry<Integer, Book> entry:map.entrySet()){
			int key = entry.getKey();
			Book b = entry.getValue();
		}
		2.Key trung se lay value cuoi cung
-Bootstrap JS Modal:
	+ https://www.w3schools.com/bootstrap/bootstrap_ref_js_modal.asp
-Ajax: xu ly bat dong bo, 
	+ allways have: type, url, data, dataType
-51.5